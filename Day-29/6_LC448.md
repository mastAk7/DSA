```cpp
class Solution {
public:
    vector<int> findDisappearedNumbers(vector<int>& nums) {
        int n = nums.size();
        vector<int> f(n,0), ans;
        for(auto x:nums) f[x-1] = 1;
        for(int i=0; i<n; i++) if(!f[i]) ans.push_back(i+1);
        return ans; 
    }
};
```